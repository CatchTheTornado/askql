import * as askjsx from '../../../askjsx';
askjsx;

export const expectedOutput = (
  <ask>
    <for
      key={<const name={'price'} type={'any'} />}
      of={<ref name={'priceList'} />}
    ></for>
  </ask>
);
