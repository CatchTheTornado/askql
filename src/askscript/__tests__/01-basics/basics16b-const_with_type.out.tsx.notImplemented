import * as askjsx from '../../../askjsx';
askjsx;

export const expectedOutput = (
  <ask>
    <const name={'a'} type={'int'} value={1} />
    <const name={'b'} type={'float'} value={1.5} />
    <const name={'c'} type={'bool'} value={false} />
    <const name={'d'} type={'string'} value={'asdf'} />
    <const name={'e'} type={'any'} value={2.3} />
  </ask>
);
