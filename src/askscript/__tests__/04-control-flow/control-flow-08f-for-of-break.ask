ask {
    const itemPrices = [10, 40, 32, 99];
    let mySum:float = 0
    for(const itemPrice of itemPrices) {
        if (itemPrice == 32) {
            break;
        }
        mySum = mySum:plus(itemPrice)
    }
    mySum
}