import { parse, runUntyped } from '../../..';
import source from './basics16d-const_with_type_without_value.ask';
import * as environment from './_environment';

test('basics16d-const_with_type_without_value', async () => {
  await expect(
    runUntyped(environment, parse(source), [])
  ).rejects.toBeDefined();
});
