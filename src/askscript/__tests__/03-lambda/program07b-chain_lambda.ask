ask {
  let useFor = fun(x, f) { f(x) }
  5 :times(2) :plus(1) :toString :useFor(fun(s:string): string {
	  const double = fun(s:string):string {
		  s:concat(s)
		}
	  s:double // same as s:double()
	}) :toInt :divideBy(6) :ceil
}