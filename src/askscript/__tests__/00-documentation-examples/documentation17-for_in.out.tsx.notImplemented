import * as askjsx from '../../../askjsx';
askjsx;

export const expectedOutput = (
  <ask>
    <for
      key={<const name={'index'} type={'any'} />}
      in={<ref name={'priceList'} />}
    ></for>
  </ask>
);
