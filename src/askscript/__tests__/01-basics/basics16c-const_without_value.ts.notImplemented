import { parse, runUntyped } from '../../..';
import source from './basics16c-const_without_value.ask';
import * as environment from './_environment';

test('basics16c-const_without_value', async () => {
  await expect(
    runUntyped(environment, parse(source), [])
  ).rejects.toBeDefined();
});
