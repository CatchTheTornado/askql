import * as askjsx from '../../../askjsx';
askjsx;

export const expectedOutput = (
  <ask>
    <set name={'sum'} type={'float'} />
    <for key={<set name={'index'} type={'int'} />} in={<ref name={'itemPrices'} />}>
      <assign name={'sum'} value={<call name={'plus'} args={[<ref name={'sum'} />,<call name={'at'} args={[<ref name={'itemPrices'} />,<ref name={'index'} />]} />]} />} />
    </for>
    <ref name={'sum'} />
  </ask>
);
