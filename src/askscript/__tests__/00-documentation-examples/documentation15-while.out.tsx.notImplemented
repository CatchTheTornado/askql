import * as askjsx from '../../../askjsx';
askjsx;

export const expectedOutput = (
  <ask>
    <while
      condition={
        <call
          name={'lessThan'}
          args={[<call name={'rand'} args={[]} />, 0.5]}
        />
      }
    >
      <call name={'doSomething'} args={[]} />
    </while>
  </ask>
);
